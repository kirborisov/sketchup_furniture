# Мебельный Конструктор для SketchUp

Модульная система для создания корпусной мебели в SketchUp с автоматическим расчётом раскроя и фурнитуры.

## Возможности

- ✅ Создание шкафов, гардеробов, встроенных систем
- ✅ Автоматический расчёт раскроя (ЛДСП, ДВП)
- ✅ Список фурнитуры
- ✅ Секции с процентным соотношением
- ✅ Полки фиксированные и регулируемые
- 🔲 Двери и фасады (в разработке)
- 🔲 Ящики (в разработке)
- 🔲 Кухонные модули (в разработке)

## Установка

### В SketchUp

```ruby
# Загрузить один раз
load "C:/путь/к/sketchup_furniture/sketchup_furniture.rb"
```

### Путь в разных ОС

```ruby
# Windows
load "C:/Users/Имя/Desktop/sketchup_furniture/sketchup_furniture.rb"

# macOS
load "/Users/Имя/Desktop/sketchup_furniture/sketchup_furniture.rb"

# Linux
load "/home/имя/Desktop/sketchup_furniture/sketchup_furniture.rb"
```

---

## Быстрый старт

### Простейший шкаф

```ruby
# Один шкаф 600×2000×400мм
cabinet = SketchupFurniture::Assemblies::Cabinet.new(600, 2000, 400, name: "Шкаф")
cabinet.build
```

### Шкаф с полками

```ruby
cabinet = SketchupFurniture::Assemblies::Cabinet.new(600, 2000, 400, name: "Шкаф") do
  shelves [400, 800, 1200, 1600]
end
cabinet.build
```

### Встроенный шкаф (несколько колонн)

```ruby
wardrobe = Wardrobe.new "Прихожая", depth: 400 do
  
  column 800 do
    base 500
    cabinet 1500
    top 700
  end
  
  column 800 do
    base 500
    cabinet 1500
    top 700
  end
  
end

wardrobe.build
wardrobe.print_cut_list
```

---

## Примеры

### Пример 1: Простой шкаф-пенал

```ruby
Wardrobe.new "Пенал", depth: 350 do
  column 400 do
    cabinet 2100, name: "Пенал" do
      shelves [300, 600, 900, 1200, 1500, 1800]
    end
  end
end.build
```

### Пример 2: Шкаф с секциями

```ruby
Wardrobe.new "Шкаф", depth: 600 do
  column 1200 do
    cabinet 2400, name: "Шкаф-купе" do
      sections "50%", "50%"          # две равные секции
      shelf 1200                      # полка на 1200мм
    end
  end
end.build
```

### Пример 3: Полноценная прихожая

```ruby
wardrobe = Wardrobe.new "Прихожая", depth: 400 do

  # ═══ ЛЕВАЯ КОЛОННА — ОБУВЬ И ОДЕЖДА ═══
  column 900 do
    
    # Обувница
    base 450, name: "Обувь" do
      sections "50%", "50%"
      shelves [200]
    end
    
    # Отделение для одежды
    cabinet 1450, name: "Одежда" do
      sections "50%", "50%"
      shelf 900                       # полка над штангой
    end
    
    # Антресоль
    top 800, name: "Антресоль", shelf: 400
  end

  # ═══ ЦЕНТРАЛЬНАЯ КОЛОННА — ПОЛКИ ═══
  column 700 do
    
    base 450, name: "Ящики" do
      shelves [150, 300]
    end
    
    cabinet 1450, name: "Полки" do
      sections "50%", "50%"
      shelves [300, 600, 900, 1200]   # 4 полки
    end
    
    top 800, shelf: 400
  end

  # ═══ ПРАВАЯ КОЛОННА — СКАМЕЙКА ═══
  column 1000 do
    
    # Скамейка с полкой для обуви
    base 450, name: "Скамейка" do
      shelf 150
    end
    
    # Открытое пространство (для крючков)
    cabinet 1450, name: "Крючки"
    
    top 800, shelf: 400
  end

end

# Построить 3D модель
wardrobe.build

# Вывести результаты
wardrobe.summary
wardrobe.print_cut_list
wardrobe.print_hardware_list
```

### Пример 4: Кладовка

```ruby
Wardrobe.new "Кладовка", depth: 500 do
  
  # Левая стенка — глубокие полки
  column 600 do
    cabinet 2400, name: "Полки" do
      shelves [400, 800, 1200, 1600, 2000]
    end
  end
  
  # Правая стенка — узкие полки
  column 400 do
    cabinet 2400, name: "Банки" do
      shelves [300, 600, 900, 1200, 1500, 1800, 2100]
    end
  end
  
end.build
```

---

## Структура проекта

```
sketchup_furniture/
│
├── sketchup_furniture.rb                 # 🚀 Главный загрузчик
│
├── core/                        # ⚙️ Ядро системы
│   ├── component.rb             # Базовый класс компонента
│   ├── config.rb                # Глобальная конфигурация
│   ├── context.rb               # Контекст построения (позиция)
│   ├── cut_item.rb              # Элемент раскроя
│   └── hardware_item.rb         # Элемент фурнитуры
│
├── primitives/                  # 📐 3D примитивы (SketchUp API)
│   └── panel.rb                 # Создание панелей
│
├── materials/                   # 🪵 Материалы
│   └── catalog.rb               # Каталог (ЛДСП, ДВП, МДФ)
│
├── components/                  # 🧩 Независимые компоненты
│   ├── body/                    # Корпусные элементы
│   │   ├── side.rb              # ✅ Боковина
│   │   ├── top_bottom.rb        # ✅ Верх/Дно
│   │   ├── back.rb              # ✅ Задняя стенка
│   │   └── shelf.rb             # ✅ Полка
│   ├── fronts/                  # 🔲 Фасады (в разработке)
│   │   ├── door.rb              # Дверь
│   │   ├── drawer_front.rb      # Фасад ящика
│   │   └── flap.rb              # Откидной фасад
│   ├── drawers/                 # 🔲 Ящики (в разработке)
│   │   ├── drawer.rb            # Ящик целиком
│   │   └── drawer_box.rb        # Короб ящика
│   ├── hardware/                # 🔲 Фурнитура (в разработке)
│   │   ├── hinge.rb             # Петля
│   │   ├── handle.rb            # Ручка
│   │   ├── slides.rb            # Направляющие
│   │   └── leg.rb               # Ножка
│   └── base/                    # 🔲 Основание (в разработке)
│       ├── legs.rb              # Ножки
│       └── plinth.rb            # Цоколь
│
├── assemblies/                  # 🏗️ Сборки
│   └── cabinet.rb               # ✅ Базовый шкаф
│
├── containers/                  # 📦 Контейнеры
│   ├── column.rb                # ✅ Вертикальная колонна
│   ├── wall.rb                  # 🔲 Стена (в разработке)
│   └── room.rb                  # 🔲 Комната (в разработке)
│
├── presets/                     # 🎨 Готовые шаблоны
│   ├── wardrobe.rb              # ✅ Встроенный шкаф
│   ├── kitchen_base.rb          # 🔲 Кухня напольная
│   └── kitchen_wall.rb          # 🔲 Кухня навесная
│
├── dsl/                         # 💬 DSL расширения (в разработке)
│
├── output/                      # 📊 Вывод результатов
│   ├── cut_list.rb              # ✅ Таблица раскроя
│   └── hardware_list.rb         # ✅ Список фурнитуры
│
├── utils/                       # 🔧 Утилиты (в разработке)
│
└── examples/                    # 📚 Примеры
    └── my_wardrobe.rb           # Пример прихожей
```

**Легенда:** ✅ реализовано | 🔲 в разработке

---

## API Reference

### Wardrobe

```ruby
Wardrobe.new(name, depth:, thickness: 18, &block)
```

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `name` | String | "Шкаф" | Название |
| `depth` | Number | 400 | Глубина (мм) |
| `thickness` | Number | 18 | Толщина ЛДСП (мм) |

**Методы:**
- `.build` — построить 3D модель
- `.print_cut_list` — вывести раскрой
- `.print_hardware_list` — вывести фурнитуру
- `.summary` — краткая сводка

### Column

```ruby
column(width, name: nil, &block)
```

Внутри блока доступны:
- `base(height, name:, &block)` — нижний модуль
- `cabinet(height, name:, &block)` — средний модуль
- `top(height, name:, shelf:)` — верхний модуль (антресоль)

### Cabinet

```ruby
cabinet(height, name:) do
  sections "50%", "50%"       # секции (проценты или мм)
  shelf 400                   # одна полка
  shelves [300, 600, 900]     # несколько полок
  no_back                     # без задней стенки
end
```

---

## Секции

Секции задаются в методе `sections`:

```ruby
# Проценты (автоматический расчёт)
sections "50%", "50%"         # две равные
sections "30%", "70%"         # 30% и 70%
sections "33%", "34%", "33%"  # три секции

# Абсолютные значения (мм)
sections 400, 400, 328        # точные размеры

# Одна секция (без перегородок)
# просто не вызывать sections
```

При изменении ширины колонны секции в процентах пересчитываются автоматически.

---

## Вывод

### Раскрой

```
===========================================================================
ТАБЛИЦА РАСКРОЯ
===========================================================================

ЛДСП 18мм:
---------------------------------------------------------------------------
Шкаф            | Деталь               |      Длина |     Ширина | Кол-во
---------------------------------------------------------------------------
Обувь           | Боковина левая       |      450 мм |      396 мм |     1
Обувь           | Боковина правая      |      450 мм |      396 мм |     1
...
---------------------------------------------------------------------------
                                                   Площадь: 17.36 м²
```

### Фурнитура

```
============================================================
СПЕЦИФИКАЦИЯ ФУРНИТУРЫ
============================================================
Полкодержатель                                        44 шт
============================================================
```

---

## Конфигурация

### Глобальные настройки

```ruby
Furniture.config.material_thickness = 18    # толщина ЛДСП
Furniture.config.back_thickness = 4         # толщина задника
Furniture.config.depth = 400                # глубина по умолчанию
```

### Материалы

Поддерживаемые материалы:
- ЛДСП 16, 18, 22 мм
- ДВП 3, 4, 6 мм
- МДФ 16, 18, 19 мм
- Фанера 10, 15, 18 мм

---

## Разработка

### Добавление нового компонента

1. Создайте файл в `components/`
2. Наследуйтесь от `SketchupFurniture::Core::Component`
3. Реализуйте `build_geometry`
4. Добавьте `add_cut` для раскроя
5. Добавьте `load` в `sketchup_furniture.rb`

```ruby
# components/hardware/handle.rb
module Furniture
  module Components
    class Handle < Core::Component
      def initialize(type: :bar, size: 128)
        super(size, 30, 30, name: "Ручка")
        @type = type
        @size = size
      end
      
      def build_geometry
        add_hardware(
          type: :handle,
          name: "Ручка рейлинг",
          size: @size,
          quantity: 1
        )
      end
    end
  end
end
```

---

## Планы развития

- [ ] Двери (распашные, откидные)
- [ ] Ящики (стандартные, тандембокс)
- [ ] Ножки и цоколь
- [ ] Петли с расчётом количества
- [ ] Ручки
- [ ] Кухонные модули
- [ ] Угловые шкафы
- [ ] Экспорт раскроя в CSV/Excel
- [ ] Визуализация раскроя на листах
- [ ] Расчёт стоимости

---

## Лицензия

MIT

---

## Автор

Создано с помощью Claude AI
